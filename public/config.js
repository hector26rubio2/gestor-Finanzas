// Configuración explícita de despliegue. Para conectar la API, cambie a:
// window.__FINANZAS_CONFIG__ = { mode: 'api', apiBaseUrl: 'https://su-api.onrender.com' };
window.__FINANZAS_CONFIG__ = { mode: 'demo' };
